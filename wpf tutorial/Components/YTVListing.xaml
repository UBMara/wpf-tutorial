<UserControl x:Class="wpf_tutorial.Components.YTVListing"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:wpf_tutorial.Components"
             xmlns:custom="clr-namespace:DropdownMenuControl;assembly=DropdownMenuControl"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border BorderBrush="{StaticResource BorderPrimary}"
            CornerRadius="5"
            BorderThickness="1">
            <Grid>
                <Border x:Name="border"
                    Background="white"
                    CornerRadius="5"/>
                <ListView ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                          SelectionMode="Single"
                          BorderThickness="0" 
                          SelectedItem="{Binding SelectedYTVListingItemViewModel}" 
                          ItemsSource="{Binding YTVListingModelsView}">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <local:YTVListingItem/>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>
    </Grid>
</UserControl>
